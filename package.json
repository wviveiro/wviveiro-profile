{
  "name": "app",
  "version": "0.1.3",
  "private": true,
  "dependencies": {
    "date-fns": "^2.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-markdown": "^4.3.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "^3.4.1",
    "slugify": "^1.4.0",
    "styled-components": "^5.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && mv ./deploy/.git ./build/ && mv ./deploy/CNAME ./build/ && mv ./deploy/.gitignore ./build/ && rm -rf ./deploy && mv ./build ./deploy && cd ./deploy && git add . && git commit -m \"New Deploy\" && git push origin master"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
